import chalk from 'chalk';
import { type LogLevelName } from './log-level';
declare type Style = typeof chalk.ForegroundColor | typeof chalk.BackgroundColor;
export declare type ColoringOptions = {
    timestamp?: Style | Style[];
    level?: {
        [key in LogLevelName]?: Style | Style[];
    };
    label?: Style | Style[];
    tags?: Style | Style[];
    message?: Style | Style[];
};
export declare type FormatOptions = {
    prelude?: boolean;
    label?: string;
    timestamp?: Date | boolean;
    level?: LogLevelName;
    tags?: Record<string, unknown>;
    color?: Style | Style[];
    colors?: ColoringOptions;
};
export declare function format(chunks: any[], { prelude, label, timestamp, level, tags, colors }?: FormatOptions): string;
export {};
